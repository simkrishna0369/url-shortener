# Claude Code

Follow `docs/agent-instructions.md` before changing code. Do not add a `CLAUDE.md` or `AGENTS.md` at the repository root.

Locked defaults: Java 17 / Spring Boot 3 / Maven / H2 file / Caffeine / `/api/v1` / **302-only** redirects / Base62(id) short codes. Coding rules: `docs/coding-standards.md`. Backlog: `docs/future-work.md`.
